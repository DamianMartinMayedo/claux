import CargandoPantalla from '@/components/CargandoPantalla'

// La lista más pesada del admin: todos los tenants con sus condiciones, el catálogo
// de módulos, las plantillas de sector y los parámetros del presupuesto, en paralelo.
export default function ClientesCargando() {
  return <CargandoPantalla titulo="Clientes" />
}
