# CLAUX — Contexto del proyecto para desarrollo
> Fuente de verdad del producto y la arquitectura. Vive en docs/ y se lee completo antes de cualquier tarea (ver AGENTS.md). Derivado del plan de negocio de junio 2026 (v2).

## 1. Qué es CLAUX

Plataforma SaaS todo en uno para digitalizar negocios locales cubanos. El vertical de lanzamiento son los restaurantes, pero la plataforma es multi-sector por configuración (gimnasios, salones de belleza, tiendas, servicios): el núcleo es el mismo y cambian la terminología y los módulos típicos, vía plantillas de onboarding por sector. Por eso la nomenclatura interna de los módulos públicos es genérica: **"catálogo digital QR"** (que en restaurantes se renderiza como "menú") y **"reservas y citas"** (mesas, citas por profesional o clases según el sector). Una sola aplicación multi-tenant: cada negocio (tenant) ve y configura únicamente los módulos que tiene activos. El cliente accede casi siempre desde móvil con conexión lenta. La IA es el eje de la comunicación comercial del producto; internamente se usa solo donde aporta (ver principios 6 y 9). El equipo opera desde España y Cuba; el hosting y los servicios externos se contratan desde España/EEUU.

## 2. Punto de partida del código — AUDITADO (junio 2026)

> **Esto es un mapa del estado actual, no un changelog.** Un bullet por módulo/área: qué es · estado · puntos de entrada (ruta · vista · acciones). Al terminar una feature se **actualiza su bullet**; prohibido añadir párrafos-ensayo, listas de migraciones o nombres de RPC aquí (eso vive en `supabase/migrations/` y el código). Los gotchas de UI viven en `skills/ui/SKILL.md`. Presupuesto de esta sección: ≤ ~1.200 palabras.

El desarrollo NO parte de cero: el repo (`DamianMartinMayedo/claux`) fue auditado.

**Stack:** Next.js 16.2.4 (App Router, TS, Server Actions; middleware en `src/proxy.ts`), React 19, Supabase (Postgres), Tailwind v4 solo como reset — el estilo real es el design system propio en `src/app/styles/` (ver `skills/ui/SKILL.md`; valores en `01-tokens.css`). Despliegue en Vercel. **jsPDF** para PDFs: vectoriales, dibujados en coordenadas desde `src/lib/pdf/` (plantilla de marca en `documento.ts`, cursor de reporte en `reporte.ts`), **no** capturas del HTML.

**Fundaciones (existen y se reutilizan):**
- **Multi-tenancy** por `client_id` en todas las tablas (sin RLS: cada query filtra por `client_id`). Doble auth: super admin con Supabase Auth (`/admin`); usuarios de cliente con `client_users` + JWT HMAC en cookie httpOnly (`/portal`). Roles: `admin_empresa`, `usuario`, `solo_lectura`.
- **Módulos conmutables (feature flags):** `clients.modulos_activos` decide qué se ve; el sidebar **oculta** lo no contratado (sin candados). Control real server-side: cada página gateada llama `requireModulo('<clave>')`. Helper `src/lib/modulos.ts` (`tieneModulo`).
- **Admin interno** (`/admin`): clientes con estados y periodo de gracia, catálogo de módulos con precios USD (`/admin/modulos`), toggle por cliente con recálculo de precio, registro de pagos, dashboard de vencimientos, auditoría y pantalla de bloqueo (`BloqueadoScreen`).
- **Portal (ERP):** multi-empresa, multimoneda con pares de tasa (CUP/MLC/USD/EUR), terceros (UI «Clientes y proveedores», ruta `/portal/terceros`, tabla `third_parties`), y **dashboard dinámico por módulos** (`/portal/dashboard`: cada módulo activo aporta su widget; builders en `dashboard.ts`; gráficos con recharts, temáticos por tokens). **Regla de moneda:** todo selector de moneda ofrece las del cliente (`monedas`), nunca una lista fija en código — un código que el cliente no tiene no cotiza (sin par ni tasa) y descuadra saldos y reportes. Guardias en `lib/tasas.ts` (`monedaValida`, `mapaTasas`, `construirConversor`); los selects conservan el valor guardado con `opcionesCon` (`components/portal/form-helpers.ts`) para no borrarlo en silencio. Cambiar de moneda una ficha convierte los importes con la tasa vigente, siempre en servidor y con confirmación.

**Base contable (módulo `base`, completa):** Ventas (facturas/ofertas, numeración `FAC-2026-####`, estados, PDF) · Tesorería (cuentas caja/banco/pasarela, movimientos, transferencias misma-moneda, saldos por moneda) · Gastos y cobros · CxC/CxP (aging en `cobranza.ts`/`CuentasView`) · Reportes financieros (`reportes.ts`/`ReportesView`: estado de resultados devengado + flujo de caja efectivo, por moneda y período; descarga PDF/CSV y **«Enviar al asesor»** por correo con adjuntos —PDF cliente + CSV técnico servidor—, consolidado opcional, directorio de asesores empresa-aware en tabla `asesores`, gestionable en el modal y en Perfil). **Liquidación unificada:** pagar/cobrar cualquier documento es un movimiento de Tesorería (`origen=PAGO/COBRO`, `referencia_id`); admite pagos **parciales**; saldo y estado (PENDIENTE/PARCIAL/LIQUIDADO; factura→COBRADA al saldar) se **derivan**, sin tabla de liquidaciones. Rutas `/portal/{ventas,tesoreria,gastos,cuentas,reportes,terceros}`, gateadas con `requireModulo('base')`. Sin partida doble (tier futuro).

**Módulo RRHH:** cinco páginas planas — **Personal** (`/portal/rrhh`, `empleados` con snapshot de contrato + baja), **Contratos** (`/portal/contratos`, historial), **Turnos** (`/portal/turnos`, planificador semanal), **Nómina simple** (`/portal/nomina`, BORRADOR/CONFIRMADA) y **Reportes** (`/portal/rrhh-reportes`, analítica en cliente). Nómina CONFIRMADA registra un gasto «Salarios» en la base contable (liquidación unificada) y guarda `gasto_id`. Acciones `rrhh.ts`; `requireModulo('rrhh')`.

**Módulo Inventario:** stock por `(producto, almacén)` en `stock_almacenes` (el ledger `movimientos_inventario` es la fuente de cada cambio: ENTRADA/SALIDA/AJUSTE/TRANSFERENCIA); `products.stock_actual` es el total. Cuatro páginas: Productos, Almacenes, **Movimientos** (`/portal/inventario`, alta manual + historial) y **Compras** (`/portal/compras`, documento cabecera+líneas, `COM-AAAA-####`). Compra CONFIRMADA sube stock y crea un gasto «Compras» en la base contable; anular revierte ambos. Mutar stock y confirmar/anular compra viven en **funciones Postgres atómicas** (seguras ante concurrencia); `inv_recalcular_stock` reconstruye desde el ledger como red de seguridad. Acciones `inventario.ts`/`compras.ts`; `requireModulo('inventario')`.

**Módulo Caja (POS offline):** punto de venta **PWA instalable** (clave `caja`, tipo módulo) que funciona **sin conexión** y sincroniza con Claux. App en `(public)/caja` (aislada del design system, IndexedDB como fuente local, `public/sw-caja.js`, instalada por enlace tokenizado `…/caja#t=<sync_token>`); endpoints `/caja/api/{seed,sync}` (semilla productos/monedas/tasas ↓, lote tickets+cierres ↑, **idempotente** por `ticket_uuid` y por los flags del cierre). Portal `/portal/caja`: hub, config (enlace de instalación + almacén + monedas→cuentas CAJA), **Operaciones** (pestañas Ventas / Movimientos de stock), Cierres y Sincronizar (subir archivo). Independiente: guarda su detalle propio (`caja_tickets`/`caja_ticket_lineas`); con `base`, cada cierre postea **un INGRESO resumen por moneda** en Tesorería (`origen='CAJA'`); con `inventario`, **un SALIDA resumen por producto** (`origen='VENTA'`; `inv_aplicar_movimiento` gana `p_permitir_negativo` para no rechazar ventas ya ocurridas). Núcleo `src/lib/caja/ingesta.ts`, acciones `caja.ts`; `requireModulo('caja')`. Migración 089. **Pendiente:** QR del enlace de instalación; edición/corrección de tickets ya sincronizados.

**Independencia de módulos (regla transversal):** cada módulo funciona solo; la base opera al 100% sin ninguno. La interacción es **aditiva en una dirección**: si el cliente tiene un módulo, aparecen conveniencias de llenado rápido en otros que lo aprovechan (p. ej. el selector de productos en las líneas de venta solo se carga si `inventario` está activo — sin él, texto libre). Nunca a la inversa.

**Reservas y Citas** (dos funcionalidades vendibles por separado, gating independiente):
- **`reservas_citas` → «Reservas»** (aforo: capacidad por franja, mesas/personas). Público `/(public)/[slug]/reservar`, panel `/portal/reservas`, bot de Telegram de botones. **Completo y en producción**; mini-web optimizada para conversión (disponibilidad en 1 query con auto-carga, atajos Hoy/Mañana, teléfono+correo obligatorios, paso de revisión antes de confirmar).
- **`agenda` → «Citas»** (agenda por recurso/profesional: 1 cita por recurso y slot, servicios con duración). Público `/(public)/[slug]/citas`, panel `/portal/citas` (acciones `src/app/actions/portal/citas.ts`), **bot de Telegram propio** e independiente del de Reservas. Los huecos salen de `recurso_horarios`: un profesional sin horario semanal no genera huecos. **Personal** por alta manual o importado de RRHH (sin que Citas dependa de RRHH). Etiqueta genérica «recurso» = «Personal» (por sector: Barbero, Cabina, Cancha…).
- **Onboarding por sector:** `clients.sector` + `plantillas_sector` resuelven etiquetas y módulos sugeridos (helper `src/lib/sector.ts`).
- **Endurecido:** lock anti-overbooking (`pg_advisory_xact_lock`), rate-limit por IP + honeypot en endpoints públicos, festivos/cierres, reglas de reserva (antelación/ventana/aforo) aplicadas dentro de las RPC en todas las vías, notificaciones dueño↔cliente por Telegram, cancelación por `token` público (`/[slug]/r/<token>`), zona horaria `America/Havana`. Slug gestionado en la pestaña Configuración. **Pendiente:** modificación por el cliente y recordatorios anti no-show (requiere cron).

**Asistente IA (addon `asistente_ia`, v1):** núcleo server-only en `src/lib/ia/` — adaptador de proveedor (OpenAI-compatible, **OpenCode Zen**; `ia_model`/`ia_api_base` en `settings`), contexto **acotado por cliente** vía `obtenerDashboard()` (scoped por `client_id` y módulos), agente del dueño, medición `ia_uso` (tope blando ~500 conv/mes) e intérprete del bot. UI: touchpoints (icono+insight) repartidos por Dashboard/Ventas/Gastos/Reportes/Inventario/RRHH/Tesorería/Catálogo, chat flotante del dueño (`IaChatWidget`) y sección en Perfil. Acciones `src/app/actions/portal/ia.ts`, gateadas con `tieneModulo('asistente_ia')`. **Telegram híbrido:** si el texto libre no encaja con los botones y el addon está activo, la IA interpreta la intención (lenguaje natural→JSON) y arranca el flujo determinista; sin addon o si falla, vuelve a los botones. Config de modelo/consumo en `/admin/configuracion`. Detalles del proveedor: memoria `opencode-zen-ia`. **Pendiente:** chat público embebido para clientes finales (tráfico anónimo, requiere medición/rate-limit propios).

**Catálogo digital QR (funcionalidad `catalogo_qr`; etiqueta visible «Catálogo digital»):** modelo propio `catalogo_categorias`/`catalogo_items`, **independiente de Inventario** (vínculo blando opcional `producto_id` solo para importar como llenado rápido; nunca dependencia). Editor `/portal/catalogo` (`CatalogoEditor.tsx`, tres pestañas: Ítems, Categorías, Configuración+QR; detalle `/portal/catalogo/[itemId]` edita en el sitio con `ItemModal`), acciones `catalogo.ts`. Público `/(public)/[slug]/catalogo` (**ISR** `revalidate=60` + `revalidatePath` desde las acciones; prioriza CDN/offline, a diferencia de reservar/citas que son `force-dynamic`), detalle por ítem `/[slug]/catalogo/[itemId]`, CTA a Reservas/Citas. **PWA/offline:** manifest por negocio + `public/sw-catalogo.js` con scope acotado a `/<slug>/catalogo/`. Imágenes optimizadas en cliente (`src/lib/imagen/comprimir.ts`, canvas→WebP) + servidor (`optimizar.ts`, sharp→WebP + miniatura). **Moneda del catálogo** (`clients.catalogo_moneda`): conversión por tasa vigente reutilizando `monedas`/`tasas_cambio` (`src/lib/tasas.ts`); si falta el par, cae a la moneda original. Descuentos (%) por ítem y por categoría (efectivo = ítem si >0, si no el de su categoría). Vista conmutable tarjetas/lista, agrupada por categoría. Stock informativo solo para el dueño si hay Inventario. Hoja pública propia `catalogo-publica.css` (paleta propia, no tokens del portal). IA de ficha: autocompletar + insight (`TipoInsight: 'catalogo'`). **Pendiente:** mini-web completa (horarios/ubicación) y chat público de IA.

**Dossier del negocio (funcionalidad `dossier`):** convierte los números del cliente en dos documentos para un tercero: una **presentación publicada como enlace web** (deck) y un **estado de resultados en PDF**. Ambos beben del **mismo snapshot congelado** (`snapshot_at`): el dueño actualiza una vez y cambian los dos, así que el PDF nunca contradice el enlace. **Independiente**: funciona entero a mano; con `base` puede **traer** los números (llenado rápido) con **fusión no destructiva** — `dossier_serie.origen` es por fila (`MANUAL|BASE`) y traer nunca pisa una fila manual sin previsualización + confirmación. Con `rrhh` precarga el Equipo; con `asistente_ia` aparecen los botones «ayúdame a escribir» (la IA solo redacta prosa: las cifras llegan al prompt ya calculadas). Color y logo son **propios del dossier**, no del negocio; de un color se deriva la paleta entera con **contraste WCAG garantizado** (`lib/dossier/paleta.ts`). Portal `/portal/dossier` (`DossierEditor`: wizard de creación + 3 pestañas Mi dossier / Presentación / Estado de resultados), público `/d/<token>` (hoja propia `.dp-*`, SVG en servidor sin recharts, `revalidate=false`, noindex + no-referrer, reveal opt-in; capability URL revocable). Lógica pura en `src/lib/dossier/` (`snapshot`, `estado`, `paleta`, `grafico`, `secciones`), PDF del estado en `src/lib/pdf/dossier.ts`, acciones `dossier.ts` (+ `redactarSeccionDossier` en `ia.ts`, donde vive el gate del addon). Migración 098. **PDF del deck: no hay generador.** Es la propia página paginada por `@page`/`@media print` en `dossier-publica.css` (16:9, un slide = una página) y `window.print()` desde `DeckReveal`; el portal lo pide con `/d/<token>?print=1`, leído en cliente para no volver dinámico un deck que es caché de por vida. Cero KB, cero red y un solo renderizado: el PDF no puede desviarse del enlace. Al tocar las animaciones de `.dp-anim`, neutralizarlas también en el bloque print — si no, el PDF sale en blanco. **Addon `multidossier`** (migración 104, solo siembra el catálogo: el modelo ya era multi-dossier): con él `/portal/dossier` es el listado (`DossierLista`) y cada dossier vive en `/portal/dossier/[dossierId]`; sin él, el editor del único, igual que antes. Gates de aplicación en `dossier.ts` (`bloqueoCrear`, `bloqueoPublicar`): sin addon, un dossier y un enlace publicado; los ya creados se siguen viendo y editando. `duplicarDossier` copia campo a campo —nunca `{...origen}`— con `token: null`, para que la copia se gane su propio enlace y no pise el del original. **Pendiente:** analítica de aperturas del enlace.

**Acceso de configuración (impersonación):** el equipo CLAUX entra al portal de cualquier cliente **sin su contraseña** para configurarlo (llave en mano). Botón «Entrar al portal» en el detalle de cliente (`AccesoUsuariosCard`) → `entrarComoCliente` (`src/app/actions/admin/impersonar.ts`) firma un JWT de portal del usuario `admin_empresa` del tenant con TTL 4 h y marca `imp:{admin_email}` en la sesión (`PortalSession`); banner persistente en el portal (`ImpersonacionBanner`) con «Salir» (`salirDeImpersonacion`, vuelve al admin). Gating `requirePermiso('clientes')`; cada entrada auditada en `audit_log` (`action='impersonar'`). La sesión `imp` se **excluye** de las métricas de uso. Sin credencial compartida ni tabla de sesiones (JWT stateless: el TTL corto es la mitigación).

**Métricas de uso (admin):** dos niveles. Infra de medición: `client_users.last_login_at` (set en `loginCliente`) + tabla agregada `uso_portal` (rollup diario por tenant×usuario×módulo + RPC atómica `uso_portal_hit`, mig. 096), instrumentada en `requireModulo`/`requireAccesoModulo` con `after()` (fire-and-forget; salta si `imp`). General en `/admin/metricas` (sección `metricas`): adopción de módulos/addons, negocios/usuarios activos (7/30 d), módulos más usados, IA y sectores. Por cliente: `UsoClienteCard` en el detalle (usuarios + último acceso, registros creados por módulo, módulos más usados). Derivadas de `clients.modulos_activos` + `created_at` de las tablas de negocio; lecturas server-only en `src/app/actions/admin/metricas.ts` (`obtenerMetricasGenerales`/`obtenerUsoCliente`). `audit_log` versionado de paso (mig. 097, cerraba una brecha: existía en prod sin DDL).

**Embudo público (landing + diagnóstico):** la raíz **es** la landing (`src/app/(landing)/page.tsx`, Server Component, **ISR** `revalidate=3600`); los módulos y sectores que muestra salen del catálogo real (`obtenerCatalogoPublico`, degrada a vacío si Supabase cae), así que activar un módulo en el admin la actualiza sin redeploy. `/diagnostico` es el formulario multi-paso + informe con CTA. Ambas comparten `Chrome.tsx` (`PublicHeader`/`PublicFooter`), `iconos.tsx` y **la misma hoja** `08-landing.css` (landing `.ld-*` arriba, diagnóstico `.dg-*` en medio: al tocar una, cuidado con la otra). A diferencia de `(public)/[slug]/*`, estas dos **sí** usan el design system completo (`globals.css` + fuentes): están sujetas al presupuesto de §3, no al aislamiento duro. **Ritmo visual de la landing:** cabecera y hero forman **un solo bloque** (`.ld-hero-zona`) con `--gradient-band` — el degradado va en la zona, no en cada uno, porque dos cajas contiguas con el mismo `linear-gradient` a 30° y distinta altura no casan y se ve la costura. Debajo, crema alternando con una **banda de color vivo** (`.ld-band-teal` → `--color-band-teal`, tono **ESTABLE en claro/oscuro** como `--gradient-band`, con texto blanco) y el CTA en `--gradient-band-alt` (el mismo par invertido). Sobre fondo de marca las tarjetas se quedan **claras** y `.btn-primary` se invierte a blanco con texto teal (es teal, y sobre el degradado —que arranca en teal— se difuminaba). **El color vivo lo dan sobre todo los acentos `.ld-ac-*` / `.ld-acento-*`** (seis pares fondo/color del DS, todos con override en dark) repartidos por módulos, sectores, valor y pasos vía `colorModulo()`/`colorSector()`: sin ellos todos los iconos acababan siendo del mismo teal. **Ojo:** `--color-purple`/`--color-indigo`/`--color-rose` valen como acento pero **no como fondo de banda** — en dark se invierten a lila/azul claro y el texto blanco encima dejaría de leerse. **Cabecera solo en la landing** (el selector cuelga de `.ld-page`; `PublicHeader` es compartido y `/diagnostico` no usa esa clase, por eso ahí sigue clara y sticky): es `fixed`, no `sticky`, porque un sticky no puede salir de su bloque contenedor y dejaría de pegarse al acabar `.ld-hero-zona`; la zona compensa el hueco con `padding-top: var(--header-h)`. Su fondo base es opaco y `LandingAnim` le añade `.is-arriba` (transparente) mientras tenga el degradado detrás. Titulares fluidos con `clamp()` (`--text-hero`, `--text-section`): **no se pisan con media queries**. **Animación:** el hero entra con `@keyframes` puro (sin JS, `backwards` → visible si no corre); el resto es reveal **opt-in** (`LandingAnim` añade `.ld-anim` y solo entonces el CSS oculta — un fallo de JS deja la página legible, no en blanco). El fondo de puntos (`DotOrb.tsx`) existe pero **está fuera del render**, pendiente de decidir. **Degradados en `in oklch`** (`01-tokens.css`): teal y ámbar son casi complementarios y en sRGB el punto medio caía en oliva sucio; el aviso de soporte de navegador y su plan B están junto al token. **Color secundario SIN decidir:** hoy «ámbar vivo» (#F59E0B, escala 500/600/700); el ocre anterior (#C97A0C) venía oscurecido para valer como texto. Se descartó coral por caer junto a `--color-error`. Candidatos vivos: ámbar vivo e índigo profundo (#4F46E5). Ojo al cambiarlo: no es solo el token — lo caribeño vive también en la crema `--color-bg` y el carbón cálido del texto, y hay assets fuera del CSS (los dos SVG del logo, favicon, `opengraph-image.tsx` con estilos inline).

**Deuda de rendimiento del embudo (§3):** la landing sirve **~188 KB gzip de JS** en 12 chunks (el mayor, 69 KB, es react-dom) — muy por encima del presupuesto de <100 KB de §3. Es el runtime de React/Next, no código propio: la página es un Server Component y sus iconos se renderizan en servidor (0 KB de cliente; lucide aporta ~4 KB). Bajarlo de verdad exige sacar la landing del runtime de React, no micro-optimizar.

**Deuda técnica:** (1) migrar el hash de `client_users` de SHA-256+salt a scrypt/argon2 antes de tener clientes reales; (2) volcar el esquema base al repo (las migraciones empiezan en 001 sobre tablas creadas a mano); (3) sin RLS por tenant: **toda query nueva filtra por `client_id`**.

**Trampa al duplicar/copiar filas (leer antes de escribir cualquier `duplicar…`/`copiar…`):** varias tablas base (`third_parties`, `products`, `sales`, `clients`, `companies`, `currencies`, `payments`, `client_users`) tienen la PK real en una columna `id uuid` (default `gen_random_uuid()`) que **convive con un código de negocio legible** (`tercero_id`, `producto_id`, `venta_id`, `client_id`, `empresa_id`, `codigo`, `pago_id`, `user_id`). El esquema del repo miente en algunos casos (ver 008_terceros.sql); **comprueba la PK real contra la BD antes de asumir nada**. Dos peligros distintos:
- **A — arrastrar `id` al copiar.** Si haces spread de una fila (`select('*')`) dentro de un `insert`, arrastras el `id` original y Postgres revienta con `duplicate key … _pkey` (fue el bug de copiar tercero a otra empresa, commit 1d6dedb). Regla: **excluye `id` del spread** (`const { id: _id, ...resto } = fila`) o lista los campos uno a uno (como hacen `duplicarOferta`/`duplicarFactura`, que por eso nunca fallaron). Aplica a TODAS las tablas con PK `id`. Y regenera el código de negocio con su `generar…Id()`, nunca lo copies.
- **B — código de negocio sin índice único.** `third_parties.tercero_id`, `products.producto_id` y `sales.venta_id` **no tienen índice único en producción** (los demás sí: `clients`, `companies`, `currencies`, `payments`, `client_users`). Hoy no hay duplicados y todos los `insert` generan códigos únicos, así que los `.single()`/`.maybeSingle()` sobre esos códigos no rompen. **Pero** si añades una vía que escriba códigos sin pasar por `generar…Id()` (importación masiva CSV, migración de datos de un cliente, seed), podrías colar códigos repetidos y esos `.single()` reventarían. Antes de esa vía: añade un único compuesto `(client_id, <código>)` — hoy se puede sin conflictos (cero duplicados verificado).

**Modelo comercial (implementado; «qué» en §5, «cómo» en [MODELO-MODULOS.md](MODELO-MODULOS.md)):** no hay planes; precio = Σ módulos activos por tarifa (fundador/estándar) desde `modulos_catalogo`; gating por `clients.modulos_activos`. La contabilidad (`base`) es un módulo opcional más (`requireModulo('base')`); todo módulo no contratado se oculta, sin candados. Ciclo `ciclo_facturacion` mensual/anual (`descuento_anual_pct`); importe = `precio_mensual_usd × ciclo`. `payments.estado` (por_confirmar/confirmado): solo los confirmados cuentan. Cliente de pago nace **DESACTIVADO**, se activa al confirmar el primer cobro de suscripción o con gracia; sin «reactivar» manual. Cambios admin→portal en vivo por Supabase Realtime (`PortalRealtimeSync`). **Gaps:** sin «cancelar suscripción»; expiración solo al abrir el admin (no hay cron).

**Dev:** bypass de login local en `src/lib/dev-auth.ts` (`NODE_ENV==='development'` + `DEV_BYPASS_AUTH=true`, inerte en prod; `createClient()` usa `service_role` con el bypass activo); portal impersonable vía `DEV_PORTAL_CLIENT_ID`. Binarios nativos macOS en cuarentena → `npm run fix-native`.

## 3. Principios de arquitectura no negociables

1. **Multi-tenant con módulos conmutables (feature flags).** Un solo código y una sola infraestructura. Cada negocio es configuración: datos propios, módulos activos, plan, canales. Activar un módulo para un tenant no debe requerir despliegue. La navegación y el onboarding muestran solo lo contratado.
2. **Una sola fuente de verdad por dato.** El menú es el ejemplo canónico: el mismo registro alimenta el menú QR público, el conocimiento del bot y la disponibilidad ligada a inventario. Cambiar un precio o marcar "agotado" en el panel del dueño se refleja en todos los consumidores del dato.
3. **Rendimiento como requisito funcional.** Todo lo público (landing, diagnóstico, menús QR, mini-webs) debe ser estático o pre-renderizado, servido por CDN, mobile-first y utilizable en 3G. Presupuesto orientativo: carga inicial de páginas públicas < 100 KB, sin frameworks pesados en el lado público. PWA con service worker: un menú ya visitado debe funcionar sin conexión. **Las rutas públicas por-negocio (`(public)/[slug]/*`: menú/catálogo, reservar, citas) son la frontera dura de rendimiento (son los enlaces que se comparten con el cliente final en conexión pésima): están AISLADAS del design system del portal — no cargan `globals.css` ni fuentes web, solo su reset mínimo (`public-base.css`) + su hoja propia. Regla completa y cómo se implementa: `skills/ui/SKILL.md` §6.**
4. **Backend fuera de Cuba.** Hosting en España/EEUU. Todas las llamadas a APIs de IA salen del servidor, nunca del cliente. Proveedor de IA principal: DeepSeek (coste bajo, sin exposición a sanciones de EEUU). Diseñar el cliente de IA como adaptador intercambiable de proveedor.
5. **Telegram-first y agnóstico de canal.** La API oficial de WhatsApp Business bloquea los números +53: no construir nada sobre ella. El bot se implementa sobre la Bot API de Telegram (cada negocio tiene su propio bot/token vía BotFather; todos los tokens apuntan al mismo webhook, que enruta por token). La lógica de negocio del bot (reservas, menú, horarios) vive separada del conector de canal, de modo que añadir chat web embebido o un futuro WhatsApp sea solo añadir un conector.
6. **Motor de bot híbrido.** Las consultas predecibles (horarios, precios, platos, ubicación) se resuelven por lógica de código contra los datos del tenant: respuesta instantánea, coste cero. La IA solo interviene en conversación libre. El bot base funciona completo por botones/teclados inline sin IA: la IA es una capa opcional (add-on de pago).
7. **Límites y medición de IA por tenant.** Registrar tokens/conversaciones por negocio y mes. El add-on tiene cupo (~500 conversaciones/mes); al acercarse, avisar internamente, nunca cortar en mitad de una conversación.
8. **Corte por impago con gracia.** El estado de pago de un tenant se gestiona manualmente desde el admin interno. La desactivación nunca es brusca: aviso → degradación → corte, con plazos configurables. Jamás apagar un menú QR en mitad de un servicio.
9. **El diagnóstico es lógica pura.** El formulario de captación es de selección (sin texto libre) y el informe se genera por reglas en código, sin IA: determinista, instantáneo, gratis.

## 4. Piezas de la plataforma

- **Público (embudo):** landing de CLAUX + formulario de diagnóstico + informe de resultados con CTA a reunión. Ultraligero.
- **Público (por negocio):** catálogo digital QR (menú en restaurantes; multi-idioma opcional) + mini-web (ubicación, horarios, fotos, link a reservas) + formulario de reservas + (premium) chat embebido con IA.
- **Panel del dueño (PWA):** edición de menú y datos del negocio, gestión de reservas (confirmar/rechazar, capacidad por franjas, no-shows), y los módulos de gestión según plan (contabilidad, inventario, RRHH, analítica…). Notificaciones al Telegram personal del dueño (reserva nueva, resumen semanal).
- **Bot de Telegram por negocio:** versión botones (base) y versión conversacional con IA (add-on).
- **Admin interno (solo equipo CLAUX):** clientes, módulos activos por cliente con toggle, plan, precio resultante (cada módulo tiene precio unitario interno; el toggle recalcula la mensualidad), estado y vencimiento de pago, consumo de IA por tenant, y gestión del periodo de gracia. Versión 1 deliberadamente mínima.

## 5. Catálogo de módulos y modelo comercial

El modelo comercial NO son planes cerrados: es **una base mensual + módulos à la carte** que suman al precio. Cada cliente tiene la base más el conjunto de módulos/funcionalidades activos; el admin interno gestiona el toggle y el precio resultante se recalcula automáticamente. Lo no contratado se muestra en el portal visible pero bloqueado, con CTA de activación (patrón ya implementado en el sidebar). Tres tipos de pieza con **una sola mecánica de on/off**: la **base** (obligatoria, el sistema contable), los **módulos** (capacidades generales) y las **funcionalidades por sector** (propias de un tipo de negocio concreto).

**Base contable (obligatoria) — $20 / $35 al mes.** Es el núcleo de CLAUX: un sistema contable completo aunque simple. Incluye:
- **Ventas:** ingresos, ofertas/presupuestos y facturas (numeración correlativa, multimoneda, PDF).
- **Gastos y cobros.**
- **Cuentas por cobrar** y **cuentas por pagar.**
- **Tesorería:** cajas/cuentas, movimientos y saldos multimoneda.
- **Reportes financieros:** estado de resultados y flujo de caja simples.
- **Terceros** (clientes y proveedores) y **multimoneda** con pares de tasa.
- Panel del negocio y soporte.

No incluye partida doble ni plan de cuentas oficial: si en el futuro hace falta, será un **tier contable avanzado** con funcionalidades nuevas, no parte de la base.

| Módulo (capacidad general) | Contenido | Precio fundador / estándar |
|---|---|---|
| Inventario | Almacenes, productos, compras, movimientos, disponibilidad conectada al catálogo | +$8 / +$14 |
| RRHH | Personal, contratos, bajas, turnos, nómina simple | +$8 / +$14 |
| Multiempresa | Varias empresas/locales con consolidación (lógica ya implementada; pasa a ser de pago) | +$12 / +$20 |
| Asistente IA | Chat con clientes (Telegram + embebido en catálogo), reservas/pedidos en lenguaje natural, consultas del dueño sobre sus módulos activos, resumen semanal | +$15 / +$25 |

| Funcionalidad por sector | Contenido | Precio fundador / estándar |
|---|---|---|
| Catálogo digital QR + mini-web | Carta/catálogo/servicios con fotos y precios por QR y enlace; mini-web pública; multi-idioma opcional | +$10 / +$18 |
| Reservas (aforo) + bot Telegram (`reservas_citas`) | Capacidad por franja (mesas/personas): formulario público, panel (confirmar/rechazar, no-shows), bot de botones, notificaciones al dueño y al cliente | +$10 / +$18 |
| Citas (agenda) + bot Telegram (`agenda`) | Agenda por recurso/profesional (1 cita por recurso y slot, servicios con duración): pensado para peluquerías, clínicas, alquileres. Panel `/portal/citas` + público `/[slug]/citas` + bot de Telegram propio (flujo conversacional de pedir cita), independiente del de Reservas. | +$10 / +$18 |
| Documentos de imprenta | El cliente envía sus documentos por correo antes de pasar a recogerlos | a definir |

Módulos/funcionalidades de fases futuras (no MVP): **contabilidad avanzada** (plan de cuentas, partida doble / modo contable oficial cubano, rol contador externo), **marketing y reseñas** (Google Business, promos), operación en sala offline-first sobre red local (comandas, pantalla de cocina) y equivalentes por vertical, gestión de repartidores, fidelización, CRM, activos fijos. Variantes "Google" como integraciones opcionales (Calendar, Sheets, Business Profile).

Implicación técnica (implementada): el modelo de "planes" cerrados se eliminó; la entidad que manda es el conjunto de módulos activos del tenant (`clients.modulos_activos`) y su precio compuesto, más su ciclo (`ciclo_facturacion`), no un tier con nombre. Los precios viven en datos gestionados desde el admin (tabla `modulos_catalogo`); nunca hardcodear precios en el producto. Detalle técnico de la mecánica en [docs/MODELO-MODULOS.md](MODELO-MODULOS.md).

## 6. Modelo de datos mínimo (orientativo)

- **Negocio (tenant):** identidad, datos públicos (nombre, dirección, geoloc, horarios, fotos, idiomas), plan, módulos activos, estado de pago, configuración del bot (token Telegram, tono), límites de IA.
- **Menú:** categorías y platos con nombre, precio, foto, descripción, **ingredientes, alérgenos y calorías** (campos necesarios para el add-on de IA; se capturan en el onboarding llave en mano), disponibilidad (agotado), traducciones.
- **Reservas:** fecha/franja, personas, datos de contacto, canal de origen (web/bot), estado (pendiente/confirmada/rechazada/no-show), capacidad por franja del negocio.
- **Base contable:** ventas (facturas/ofertas), terceros y monedas ya existen y se reutilizan. Pendientes de construir: gastos/cobros, cuentas por cobrar/pagar, tesorería y reportes financieros (reutilizando los patrones de ventas: numeración correlativa, estados, multimoneda).
- **Inventario / RRHH:** Productos y Almacenes (ya construidos) pertenecen al **módulo inventario**, no a la base. Las líneas de factura de la base se introducen a mano; el módulo inventario añade el selector de productos sobre el mismo editor (`_DocumentoLineasEditor` ya soporta ambos modos vía `datalist`). RRHH es módulo aparte (personal, contratos, bajas, turnos, nómina simple).
- **Uso de IA:** registro por tenant, mes, conversaciones y tokens.

## 7. Restricciones de contexto (Cuba) que condicionan decisiones técnicas

Conectividad móvil lenta y cara (ETECSA), cortes eléctricos frecuentes, usuarios 95% en móvil, pagos del cliente final gestionados fuera de la plataforma (cobro manual en USD/EUR vía cuentas en EEUU/España — la plataforma NO procesa pagos, solo refleja el estado), WhatsApp Business API inutilizable para +53, Telegram de adopción masiva. El idioma de toda la interfaz es español (menús públicos además en inglés para turismo). Cualquier decisión que aumente peso de página, dependencia de conexión continua o latencia percibida debe justificarse.

## 8. Prioridades de construcción (Fase 0 — MVP comercializable)

La auditoría (sección 2), el cascarón multi-tenant y el admin interno ya existen. El **sistema real de módulos à la carte ya está implementado** (migraciones 017 + 018; planes eliminados). El eje pendiente de la primera ola es **convertir la base en un sistema contable completo**; la capa pública vertical de restaurantes viene después. Orden:

1. Deuda técnica previa: migrar hash de contraseñas a scrypt/argon2 y volcar el esquema base de la BD al repo.
2. ~~**Sistema de módulos à la carte**~~ **(HECHO)**: catálogo `modulos_catalogo` (`tipo` módulo/funcionalidad/addon; la contabilidad es un módulo más, clave `base`), columnas `clients.modulos_activos`/`tarifa`/`ciclo_facturacion`/`precio_mensual_usd`, gating del portal leído del cliente, admin con toggle por módulo y recálculo de precio (`/admin/modulos` + tarjeta en detalle de cliente), ciclo mensual/anual con descuento, pago único de configuración, y planes eliminados. Detalle técnico en [docs/MODELO-MODULOS.md](MODELO-MODULOS.md).
3. ~~**Completar la base contable**~~ **(HECHO — Fase 4 terminada):** gate del selector de productos por Inventario · Tesorería · Gastos y cobros · CxC/CxP + cobro de facturas (aging; liquidación unificada parcial vía Tesorería) · Reportes financieros (estado de resultados devengado + flujo de caja efectivo, por moneda y período). La base contable está completa.
4. ~~Modelo de datos del vertical: crear catálogo (categorías/platos con ingredientes, alérgenos, calorías, foto, agotado, traducciones) y reservas.~~ **(HECHO — catálogo en §2 «Catálogo digital QR construido»; reservas en el punto 6.)** Pendiente: extender el tenant con el resto de datos públicos del negocio (horarios/ubicación editables) para la mini-web completa.
5. ~~Catálogo QR + mini-web pública (rutas públicas estáticas/ISR, presupuesto de rendimiento de la sección 3) + edición en el portal del dueño.~~ **(HECHO — ver «Catálogo digital QR construido» en §2: editor en `/portal/catalogo`, público `/[slug]/catalogo` con ISR, imágenes optimizadas cliente+servidor, QR, PWA/offline. Mini-web completa queda pendiente, ver punto 4.)**
6. ~~Reservas: formulario público + panel en el portal + notificación Telegram al dueño.~~ **(HECHO — modo aforo `reservas_citas` y modo agenda `agenda`, endurecidos; panel `/portal/citas` + público `/[slug]/citas`; ver §2.)**
7. ~~Bot de Telegram de botones (reservar, catálogo, horarios, ubicación) con enrutado multi-tenant por token.~~ **(HECHO — bot de botones multi-tenant por token, con `secret_token`, dedupe de updates y notificaciones dueño↔cliente; bots independientes Reservas/Citas con su flujo conversacional propio; ver §2.)** Pendiente: completar `carta`/`ubicación` (hoy stubs).
8. ~~Landing de CLAUX + formulario de diagnóstico + informe (embudo).~~ **(HECHO — ver «Embudo público» en §2: la raíz es la landing con ISR y catálogo real; `/diagnostico` es el formulario + informe.)**
9. Build-out de los módulos restantes: ~~**RRHH**~~ **(HECHO — Fase 5: Personal + Nómina simple integrada con la base contable vía gasto «Salarios»)**; ~~**Inventario**~~ **(HECHO — Fase 5: stock por almacén + Movimientos + Compras integradas vía gasto «Compras»)**; ~~add-on **IA** v1~~ **(HECHO — ver «Asistente IA construido» en §2)**; pendiente **Multiempresa** (consolidación + gating de pago).

Regla general: ante la duda entre hacerlo perfecto o hacerlo vendible, vendible — pero nunca a costa de los principios de la sección 3, que son los que evitan rehacer la casa después.
